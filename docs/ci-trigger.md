# CI Trigger

This file exists to trigger the repository CI workflow after workflow configuration changes.

CI validates simulation tests, simulation typechecking, the web build, and required project files.
