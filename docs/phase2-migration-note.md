The Phase 2 database migration is deployed to the connected Supabase project. It adds request-key idempotency, authoritative state versions, internal competition events, locked lifecycle RPCs, private submission reads, and removal of browser mutation privileges. The browser submission client now supplies a fresh UUID request key per attempt so network retries can safely replay the same mutation.
