Phase 2 uses the existing competition schema and RPC architecture rather than introducing a parallel service. Database row locks provide serialization for room lifecycle and round transitions; unique constraints protect duplicate submissions; request keys provide idempotent retries; RLS limits browser reads; state_version and competition_events provide authoritative change tracking.